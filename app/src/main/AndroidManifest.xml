<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="fm.ps.annat.travel.pilgrism">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_logo"
        android:label="@string/application_name"
        android:roundIcon="@drawable/ic_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.AnnatTravelPilgrism">

        <!--
             TODO: Before you run your application, you need a Google Maps API key.

             To get one, follow the directions here:

                https://developers.google.com/maps/documentation/android-sdk/get-api-key

             Once you have your API key (it starts with "AIza"), define a new property in your
             project's local.properties file (e.g. MAPS_API_KEY=Aiza...), and replace the
             "YOUR_API_KEY" string in this file with "${MAPS_API_KEY}".
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="YOUR_API_KEY" />

        <activity
            android:name=".views.activities.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="fm.ps.annat.travel.pilgrism" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="MainActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.NavigationActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="fm.ps.annat.travel.pilgrism" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="NavigationActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.SignInActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="fm.ps.annat.travel.pilgrism" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="SignInActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.OnBoardingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="fm.ps.annat.travel.pilgrism" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="OnBoardingActivity" />
            </intent-filter>
        </activity>
        <activity
            android:name=".views.activities.SplashScreenActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>